<nav class="navbar navbar-expand-lg bg-light">
    <div class="container">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("cow") }}">Page A</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app") }}">Page B</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("statistic") }}">Statistic</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/reports">Reports</a>
                    </li>
                {% endif %}
            </ul>

            {% if app.user %}
                <span class="navbar-text me-2">User: {{ app.user.username }}</span>
                <a class="btn btn-primary" href="{{ path("logout") }}">Log out</a>
            {% else %}
                <div class="d-flex">
                    <span class="navbar-text">
                        <a class="nav-link me-2" href="{{ path("registration") }}">Registration</a>
                    </span>
                    <a class="btn btn-primary" href="{{ path("login") }}">Log in</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>